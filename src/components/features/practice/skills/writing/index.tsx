"use client";
import BasicBox from "@/components/base/MaterialUI-Basic/Box";

type PracticeWritingProps = {};

const PracticeWriting = (_: PracticeWritingProps) => {
  // The SelectTopic component in the layout handles exam selection
  return <BasicBox />;
};

export default PracticeWriting;
